#!/bin/bash

# Book Club Update Script
# Simple wrapper for the Node.js CLI tool

cd "$(dirname "$0")"

if [ ! -f "update-book.js" ]; then
    echo "❌ Error: update-book.js not found"
    exit 1
fi

if [ ! -d "node_modules" ]; then
    echo "📦 Installing dependencies..."
    npm install
fi

node update-book.js "$@"